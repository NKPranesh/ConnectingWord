"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = colorContinuous;

var _d3Scale = require("d3-scale");

var _palette = _interopRequireWildcard(require("./palette"));

var _utils = require("./utils");

function colorContinuous({
  attr,
  domain,
  colors = _palette.DEFAULT_PALETTE,
  nullColor = _palette.NULL_COLOR
}) {
  (0, _utils.assert)(Array.isArray(domain), 'Expected "domain" to be an array of numbers');
  const palette = typeof colors === 'string' ? (0, _palette.default)(colors) : colors;
  const color = (0, _d3Scale.scaleLinear)().domain(domain).range(palette);
  return d => {
    const value = (0, _utils.getAttrValue)(attr, d);
    return Number.isFinite(value) ? color(value) : nullColor;
  };
}
//# sourceMappingURL=color-continuous-style.js.map